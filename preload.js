// preload.js: Runs in a secure, isolated context before the renderer loads.
//   • Ideal place to expose safe APIs via contextBridge.
//   • Currently empty, reserved for future IPC or helper functions.


window.addEventListener('DOMContentLoaded', () => {
    // nothing here yet
});
